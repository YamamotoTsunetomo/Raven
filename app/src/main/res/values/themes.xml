<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Raven" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <item name="android:colorPrimary">@color/font_main</item>
        <item name="android:colorPrimaryDark">@color/font_main</item>
    </style>

    <style name="Theme.Raven" parent="Base.Theme.Raven" />
</resources>